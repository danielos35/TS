// Array de numeros

let arrayNumber = [1,2,3,4]; 
console.log(arrayNumber);

// Array String

let arrayString = ['Hola', 'Mundo']; 
console.log(arrayString);

// Array letras 

let arrayLetra = ['HolaMundo']
console.log(arrayLetra[0][0]);

// Tamaño de un string
console.log(arrayLetra[0].length);

// Declarar mediante objetos String
let objetoArray:String[] = ['Hola','Mi','Nombre','es','Daniel']; 
console.log(objetoArray);

// Declarar mediante objetos numericos
let objetoNumerico:Number[] = [1,2,3,4,56]
console.log(objetoNumerico);


// Arreglo con cualquier tipo de objetos
let anyObject = [true,"Hola", 12]; 
console.log(anyObject);

        //Siempre indicar el tipo de array que sera usado.
let anyObject2: any[] = [false, 12, "HolaMundo"]; 
console.log(anyObject2);
 













